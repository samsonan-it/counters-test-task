# Spring Boot MVC / Security / JPA Demo

## Description

Тестовое задание на тему Spring + JPA/Hibernate

## How To Start

1. В application.properties прописать путь к ПУСТОЙ БД PostgreSQL, при необходимости там же указать пользователя и пароль БД, если они не дефалтовые
2. Запустить приложение. Вся инициализация должна произойти автоматом
3. Пользователи (они создаются когда прогоняется скрипт инициализации БД): demo/demo (USER), user/user (USER), admin/admin (ADMIN)

## Особенности

Данный проект делался часов 8. Это полностью рабочая система, которая работает как через UI, так и через REST (соответственно 2 конфигурации для Spring Security). 
Так как REST писался уже по остаточному принципу, на него времени почти не оставалось, поэтому он получился лаконичным.
Какие-то моменты, которые я бы хотел сделать, не не успел, я пометил TODO

Тестирование:
- Юнит тестов нет, т.к. логика везде предельно простая, зато есть полные интеграционные тесты (от вызова метода контоллера до изменений/чтения в БД). 
Для системы в таком виде, в котором она сейчас находится, только они и имеют смысл. 
- Тестов всего 4, опять же, всвязи с ограничением по времени. Все остальные тесты делались  бы абсолютно аналогично.
- Чтобы интеграционные тесты запускались, надо создать пустую тестовую базу и в application-test.properties прописать к ней путь.